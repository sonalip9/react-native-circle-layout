import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    bottom: 0,
    elevation: 3,
    left: 0,
    position: 'absolute',
    right: 0,
  },
  flex: { flex: 1 },
  icon: { marginRight: 0 },
});
